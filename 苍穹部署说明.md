### 1、商城前端代码更新
```
### 10.11.251.11操作
#更新、解压包
cd /data/nasdata/staticresource && svn update && rm -rf b2b-pc/ b2b-mob/ && unzip b2b-pc.zip -d b2b-pc && unzip b2b-mob.zip -d b2b-mob

#更新b2b-pc
rm -rf /usr/share/nginx/b2b-pc/index.html /usr/share/nginx/b2b-pc/static && cp -rp /data/nasdata/staticresource/b2b-pc/* /usr/share/nginx/b2b-pc/

#更新b2b-mob
rm -rf /usr/share/nginx/b2b-mob/* && cp -rp /data/nasdata/staticresource/b2b-mob/* /usr/share/nginx/b2b-mob/


### 10.11.251.12操作
#更新b2b-pc
rm -rf /usr/share/nginx/b2b-pc/index.html /usr/share/nginx/b2b-pc/static && cp -rp /data/nasdata/staticresource/b2b-pc/* /usr/share/nginx/b2b-pc/

#更新b2b-mob
rm -rf /usr/share/nginx/b2b-mob/* && cp -rp /data/nasdata/staticresource/b2b-mob/* /usr/share/nginx/b2b-mob/
```
### 2、苍穹后端代码更新
```
### 10.11.251.11操作
cd /data/nasdata/appstatic/appstore/ierp && svn update

### 10.11.60.11操作
kubectl -n 374787 delete pod $(kubectl -n 374787 get pod -l app=mservice15|awk '{print $1}')
kubectl -n 374787 delete pod $(kubectl -n 374787 get pod -l app=mservice15-qing|awk '{print $1}')
kubectl -n 374787 delete pod $(kubectl -n 374787 get pod -l app=web15|awk '{print $1}')
```
### 3、苍穹元数据更新
```
查看jira任务，是否需要部署元数据，如果需要，通知相关开发人员登录苍穹开发平台导入元数据。
```
### 4、苍穹sql脚本执行
```
查看jira任务，是否需要执行sql，如果需要，则在10.11.251.11操作
cd /data/nasdata/sql_file/sql && svn update
cd xxx项目/xxx任务
mysql -unext -pxxxxxx -h10.10.6.11 < xxx.sql
```
### 5、苍穹全面预算系统代码更新
```
(1)从jira任务下载对应更新包
(2)10.11.251.11操作
cd /data/nasdata/appstatic/patch
mv ierpV1.x-latest ierpV1.x-latest_`date +'%Y%m%d%H%M'`
mkdir ierpV1.x-latest && cd ierpV1.x-latest
(3)上传更新包到此目录并解压
unzip xxx.zip && rm -f xxx.zip
(4)登录mc：http://10.11.251.10:8888/mc/，点击<集群管理>--<生产环境>--<升级>，执行升级操作
```
### 6、苍穹配置更新
```
登录mc：http://10.11.251.10:8888/mc/，根据jira任务描述更新配置
```